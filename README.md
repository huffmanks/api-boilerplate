# API boilerplate

Starter express and mongoose api server.

## Getting Started

To get a local copy up and running follow these simple example steps.

1. Clone the repo
    ```sh
    git clone https://github.com/huffmanks/api-boilerplate.git
    ```
2. Install NPM packages
    ```sh
    yarn
    ```
